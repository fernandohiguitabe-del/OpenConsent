<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.OpenConsentDemo">

        <!-- MainActivity.kt — punto de entrada, decide si ir a Consent o Home -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Pantalla de consentimiento inicial (sin switch Usuario/Admin) -->
        <activity
            android:name=".ConsentActivity"
            android:exported="false" />

        <!-- Panel de privacidad con switch Usuario/Admin (desde ajustes) -->
        <activity
            android:name=".PrivacyPanelActivity"
            android:exported="false" />

        <!-- Home de la app demo de películas -->
        <activity
            android:name=".HomeActivity"
            android:exported="false" />

    </application>

</manifest>